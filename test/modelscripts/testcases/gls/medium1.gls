glossary model Medium1
    | ceci `Trois` la description de `Un` élément
    | dans `Un` contexte `pas` et `Trois`
    | `un` `test`

Trois
    | a `Trois`

Un


Reference
    |
    | a `à` b `une` `reference` est un peu plus que