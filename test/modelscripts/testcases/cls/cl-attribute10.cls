class model Cl_class03

class A
    attributes
        / a : Integer
        % c : Integer
        - d : Integer
        + e : Integer
        ~ f : Integer
        g : Integer {id}
        h : Integer {readOnly}
