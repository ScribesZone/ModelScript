class model Cl_class03

class A
    attributes
        /  a : Integer {id}
        / - b : Integer {id}
        % c : Integer {id,readOnly}
        - d : Integer
        + e : Integer
        ~ f : Integer
        g : Integer {id}
        h : Integer {readOnly}
