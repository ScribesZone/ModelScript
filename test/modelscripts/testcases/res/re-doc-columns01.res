    #@Issue txt.TermNotFound 2
    #@Issue else *

relation model M
import glossary model from 'g01.gls'

relation R
    columns
        t : String
            | `Un` chiffre sur `Deux`
            | and various lines
            | and with `Nothing`
        c : Integer
        d : Real
            | `Un` chiffre sur `Deux`
            | and various lines
            | and with `Nothing`