scenario model Koinv04
import class model from 'main.cls'

scenario begin
    ! toufik := new Employee
    ! havefun2 := new Project
    ? check
    -- cardinality: 'toufik' WorksIn must fail
    -- cardinality: 'havefun2' ~Controls must fail
scenario end