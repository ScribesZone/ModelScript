    //@Issue txt.TermNotFound 1
    //@Issue else *

scenario model Sc_main01

import glossary model from 'g01.gls'

context
    | `Un` chiffre sur `Deux`
    | and various lines
    | and with `Nothing`
