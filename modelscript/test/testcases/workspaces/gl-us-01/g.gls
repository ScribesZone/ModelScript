glossary model AGlossary

Term
    | Term is a term

AnotherTerm
    | Term is a term

Something
    | The definition of `Something` relates
    | to a `Term` which relates to
    | `AnotherTerm`

Two
    | Two is just like `Something`
